{
  "name": "movie-search",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js --ignore client",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "dotenv": "8.2.0",
    "express": "4.17.1",
    "node-fetch": "2.6.1"
  }
}
